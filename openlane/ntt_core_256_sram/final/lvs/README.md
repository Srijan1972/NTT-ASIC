# LVS Artifacts

This folder contains the generated LVS status report for the final SRAM macro GDS run.

Netgen did not complete, so this is not a clean LVS pass report. See `lvs_status_report.md` for the exact attempts, run tags, inputs, and failure points.
